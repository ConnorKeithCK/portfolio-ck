import React from "react";
import ToolbarStyle from "../styles/Toolbar.module.css"

function Toolbar() {

    return (
        <div className={ToolbarStyle["toolbar"]}></div>

        // TODO: Home icon top left, other icons?
    )
}

export default Toolbar